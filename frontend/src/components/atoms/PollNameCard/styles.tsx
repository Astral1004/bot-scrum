import styled from 'styled-components';

export const StyledPollNameCard = styled.div(({}) => ({
  fontSize: 14,
  fontWeight: 700,
  textAlign: 'left',
  color: '#003768',
  paddingTop: 15,
  marginBottom: 20
}));
